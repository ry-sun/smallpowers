interface:
  display_name: "Restore Regular Workspace"
  short_description: "Restore one checkout to a regular workspace"
  default_prompt: "Use $restore-regular-workspace to preview restoring /absolute/container/path."

policy:
  allow_implicit_invocation: false
