interface:
  display_name: "Work In"
  short_description: "Reuse or create a branch worktree"
  default_prompt: "Use $work-in with a branch name to reuse or create its worktree and bind this task to it."

policy:
  allow_implicit_invocation: false
