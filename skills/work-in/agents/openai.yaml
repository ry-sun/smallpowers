interface:
  display_name: "Work In"
  short_description: "Locate, reuse, or create a branch worktree"
  default_prompt: "Use $work-in with a branch name to locate or create its worktree, track a matching remote branch, and bind this task to it."

policy:
  allow_implicit_invocation: false
