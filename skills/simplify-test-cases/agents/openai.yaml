interface:
  display_name: "Simplify Test Cases"
  short_description: "Reduce tests while preserving critical behavior"
  default_prompt: "Use $simplify-test-cases to simplify the current repository's tests or the scope I name."

policy:
  allow_implicit_invocation: false
