interface:
  display_name: "Setup Worktree Workspace"
  short_description: "Convert a checkout to a worktree workspace"
  default_prompt: "Use $setup-worktree-workspace to convert /absolute/repository/path after confirmation."

policy:
  allow_implicit_invocation: false
