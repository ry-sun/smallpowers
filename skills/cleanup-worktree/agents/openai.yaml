interface:
  display_name: "Cleanup Worktree"
  short_description: "Safely remove one merged local worktree"
  default_prompt: "Use $cleanup-worktree to remove this clean, integrated worktree and update the primary branch."

policy:
  allow_implicit_invocation: false
