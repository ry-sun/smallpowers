interface:
  display_name: "Smallpowers Audit"
  short_description: "Audit a repository for removable complexity"
  default_prompt: "Use $smallpowers-audit to audit this repository for removable complexity without changing it."

policy:
  allow_implicit_invocation: false
